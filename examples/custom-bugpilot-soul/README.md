# Example: Custom BugPilot Soul

This example demonstrates how to extend the `Agent` (builtin agent loop) to customize its behavior and use it with the `Shell` UI.

```sh
cd examples/custom-bugpilot-soul
uv sync --reinstall
uv run main.py
```
