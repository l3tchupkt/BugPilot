# Example: BugPilot Wire Messages

This example demonstrates how to create and run a BugPilot instance with raw Wire message output.

```sh
cd examples/bugpilot-wire-messages
uv sync --reinstall
uv run main.py
```
